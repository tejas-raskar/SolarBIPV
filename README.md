## Local Development
- 'git clone https://github.com/tejas-raskar/SolarBIPV.git`
- Make sure node is installed on your system. If not refer https://nodejs.org/en
- `cd` into the project directory and run `npm install`
- `npm run dev` and the project will start on `localhost`
